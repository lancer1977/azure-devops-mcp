services:
  ado-mcp:
    image: ghcr.io/lancer1977/azure-devops-mcp:latest
    ports:
      - "${PORT:-8080}:8080"
    env_file:
      - .env
    environment:
      - PORT=8080
    restart: unless-stopped